{
  "name": "seele-web",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "description": "SEELE landing page — Astro 6 static site",
  "license": "MIT",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "visual": "node scripts/visual-critique.mjs",
    "visual:preview": "node scripts/visual-critique.mjs --url http://localhost:4321/seele"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "dependencies": {
    "astro": "^6.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "sharp"
    ]
  }
}
