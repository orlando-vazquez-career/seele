//! SEELE sync — git-friendly compressed chunks for multi-machine memory sync.
//!
//! Implementation lands in sprint-04.

#[cfg(test)]
mod tests {
    #[test]
    fn smoke() {
        assert!(true);
    }
}
