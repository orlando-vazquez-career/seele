//! SEELE storage — SQLite + FTS5 + sqlite-vec layer.
//!
//! Implementation lands in sprint-01 bloque-C.

pub mod vec0_loader;

#[cfg(test)]
mod tests {
    #[test]
    fn smoke() {
        assert!(true);
    }
}
