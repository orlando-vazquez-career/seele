//! SEELE project detection — 5-case algorithm for inferring project name from cwd.
//!
//! Implementation lands in sprint-04.

#[cfg(test)]
mod tests {
    #[test]
    fn smoke() {
        assert!(true);
    }
}
