//! SEELE MCP server — stdio transport for Claude Code, Cursor, OpenCode, etc.
//!
//! Implementation lands in sprint-03.

#[cfg(test)]
mod tests {
    #[test]
    fn smoke() {
        assert!(true);
    }
}
